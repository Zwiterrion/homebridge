import React from 'react';

export const NotFound = React.createClass({
  render() {
    return (
      <h2>Il n'y a qu'une seule page sur cette appli donc ne cherchez pas ailleurs ;)</h2>
    );
  }
});
