import React from 'react';
import Mic from './mic';

const AccessoryApp = React.createClass({

  render () {
    return (
      <div>
        <Mic />
      </div>
    );
  }
})

export default AccessoryApp
